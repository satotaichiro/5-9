
<h2>Books</h2>

	<table>
	<tr>
		<th>Title</th>
		<th>Opinion</th>
	</tr>

	<!--取得・一覧のリストをここに出す-->
	<% @books.each do |book| %>
	<tbody>
	<tr>
		<td><%= book.Title %></td>
		<td><%= book.body %></td>
		<td><%= link_to "show", book_path(book.id) %>
		</td>
	</tr>
	</tbody>
	<% end %>
</table>

<h2>New book</h2>
<%= form_for(@book) do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_field :body %>

<%= f.submit 'Create Book' %>
<% end %>